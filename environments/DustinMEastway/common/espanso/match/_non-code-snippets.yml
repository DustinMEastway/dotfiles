# Snippets that should not be used in code editors.

global_vars:
matches:
  -
    label: "Print today's date."
    replace: "{{dateToday}}"
    trigger: ":today"
  -
    label: "Print yesterday's date."
    replace: "{{dateYesterday}}"
    trigger: ":yesterday"
